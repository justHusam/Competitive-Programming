#include <bits/stdc++.h>
using namespace std;

int main(int argc, char **argv) {
	int T;
	long long r;
	for (int it = 1; it <= T; it++) {
		scanf("%lld", &r);
		printf("Case %d: %lld.25\n", it, 4 * r * r);
	}
	return 0;
}
