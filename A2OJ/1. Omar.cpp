#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
	int cases, x, y;
	scanf("%d", &cases);
	while (cases-- != 0) {
		scanf("%d%d", &x, &y);
		printf("%d\n", x + y);
	}
	return 0;
}
