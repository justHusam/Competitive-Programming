#include <bits/stdc++.h>
using namespace std;

int main(int argc, char **argv) {
	int T;
	long long n;
	scanf("%d", &T);
	while (T-- != 0) {
		scanf("%lld", &n);
		printf("%lld\n", n / 2);
	}
	return 0;
}
